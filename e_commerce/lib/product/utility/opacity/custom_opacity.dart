enum CustomOpacity {
  // Represents 10% opacity
  perCent10(0.1),
  // Represents 15% opacity
  perCent15(0.15),

  // Represents 20% opacity
  perCent20(0.2),
  // Represents 25% opacity
  perCent25(0.25),

  // Represents 30% opacity
  perCent30(0.3),
  // Represents 35% opacity
  perCent35(0.35),

  // Represents 40% opacity
  perCent40(0.4),
  // Represents 45% opacity
  perCent45(0.45),

  // Represents 50% opacity
  perCent50(0.5),
  // Represents 55% opacity
  perCent55(0.55),

  // Represents 60% opacity
  perCent60(0.6),
  // Represents 65% opacity
  perCent65(0.65),

  // Represents 70% opacity
  perCent70(0.7),
  // Represents 75% opacity
  perCent75(0.75),

  // Represents 80% opacity
  perCent80(0.8),
  // Represents 85% opacity
  perCent85(0.85),

  // Represents 90% opacity
  perCent90(0.9),
  // Represents 95% opacity
  perCent95(0.95),

  // Represents 100% opacity
  perCent100(1),
  ;

  const CustomOpacity(this.value);

  final double value;
}
